{
  "name": "hopf-fibration",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "parcel build index.html --no-source-maps --out-dir public --public-url .",
    "serve": "parcel index.html --open"
  },
  "dependencies": {
    "three": "^0.108.0",
    "three-orbit-controls": "^82.1.0",
    "vectors": "^0.1.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.3"
  }
}
